import { baseUrl } from "../../../configs/config";

export const applicationEndpoint = {
      getApplications: `${baseUrl}user/get-applications`,
      getSubmitedApplications: `${baseUrl}user/get-submited-applications`,
};
